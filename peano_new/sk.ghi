param (PROP prop.ghi () "")
param (EQ_MIN eq_min.ghi (PROP) "")

tvar (wff ph ps ch)
tvar (val A B C)

term (val (s))
term (val (k))
term (val (. A B))

stmt (ax-k () ()
  (= (. (. (k) A) B)
     A)) 
stmt (ax-s () ()
  (= (. (. (. (s) A) B) C)
     (. (. A C) (. B C))))

stmt (aeq1 () () (-> (= A B) (= (. A C) (. B C))))
stmt (aeq2 () () (-> (= A B) (= (. C A) (. C B))))
